from django import forms
'''
class CodeForm(forms.Form):
    code = forms.CharField(label='Код по МКБ-10', widget=forms.TextInput(attrs={'color':'white'}))

    def clean_info(self):
        data = self.cleaned_data['code']

        # Remember to always return the cleaned data.
        return data
'''
